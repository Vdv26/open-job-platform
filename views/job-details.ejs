<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= job.title %> - FindMyService</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>FindMyService</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/post-job">Post Job</a>
      <a href="/browse-jobs">Browse Jobs</a>
    </nav>
  </header>
  <main>
    <div class="job-details">
      <h2><%= job.title %></h2>
      <div class="job-meta">
        <span class="location"><%= job.location %></span>
        <span class="price">₹<%= job.payment %></span>
      </div>
      <div class="job-description">
        <p><%= job.description %></p>
      </div>
      <% if (job.acceptedBy) { %>
        <p class="accepted-by"><strong>Accepted By:</strong> <%= job.acceptedBy %></p>
      <% } else { %>
        <div class="accept-job-form">
          <h3>Accept this Job</h3>
          <form action="/job/<%= job._id %>/accept" method="POST">
            <div class="form-group">
              <label for="acceptedBy">Your Name:</label>
              <input type="text" id="acceptedBy" name="acceptedBy" required>
            </div>
            <button type="submit" class="btn btn-primary">Accept Job</button>
          </form>
        </div>
      <% } %>
    </div>
  </main>
</body>
</html>
